# Simple test - no curl, just testing Flux::run

Flux::Suite "Basic Tests" {
    Flux::Case "Simple assertion" {
        puts "Inside test case"
        Flux::eq 1 1
        puts "Test passed"
    }
}

puts "About to call Flux::run..."
