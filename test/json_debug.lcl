# Debug JSON parsing

GET https://httpbin.org/get {
    on_response {
        puts "Status: $status_code"
        puts "JSON type check..."
        puts $json
        puts "Trying json_at..."
        let url [Flux::json_at $json "/url"]
        puts "URL: $url"
    }
}
