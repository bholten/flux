# Debug JSON parsing

GET https://httpbin.org/get {
    on_response {
        puts "Status: $status_code"
        puts "Content-Type: $content_type"
        puts "Body length: [String::length $body]"
        puts "Body first 100 chars:"
        puts [String::range $body 0 100]
    }
}
