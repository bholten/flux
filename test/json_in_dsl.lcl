# Test json parsing via DSL

GET https://httpbin.org/get {
    on_response {
        Flux::eq $status_code 200
        puts "JSON empty?: [empty? $json]"
        if [not [empty? $json]] {
            let url [get $json "url"]
            puts "URL from JSON: $url"
        }
    }
}
